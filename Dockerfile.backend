FROM gradle:4.10.3-jdk11 AS build
WORKDIR /workspace
COPY api/ ./
RUN chmod +x gradlew && ./gradlew clean bootJar -x test

FROM eclipse-temurin:11-jre
WORKDIR /app
COPY --from=build /workspace/build/libs/*.jar app.jar
EXPOSE 7070
ENTRYPOINT ["java", "-jar", "app.jar"]
